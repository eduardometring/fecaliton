import { ICommand} from "wokcommands";

export default {
    category: 'Fun',
    description: 'epic embed failure, laugh at this user',
    aliases: ['embed', 'epicfailure', 'epicembedfail', 'fail', 'failure'],

    callback: async ({message}) => {
        
    }
} as ICommand