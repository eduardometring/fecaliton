import { ICommand} from "wokcommands";

export default {
    category: 'Fun',
    description: 'zyzz chest',
    aliases: ['boobs', 'breasts'],

    callback: async ({message}) => {
        
    }
} as ICommand