import { ICommand} from "wokcommands";

export default {
    category: 'Fun',
    description: 'Use this to hug someone',
    aliases: ['abraçar', 'h'],

    callback: async ({message, args}) => {
        
    }
}  as ICommand
