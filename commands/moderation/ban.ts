import { ICommand } from "wokcommands";

export default {
    category: "Moderação",
    description: "Banir membros do seu servidor",
    aliases: ['banir'],

    callback: async ({message}) => {

    }
} as ICommand